<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.todo.mapper.TaskMapper">

    <resultMap id="TaskResultMap" type="com.example.todo.entity.Task">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="startDate" column="start_date"/>
        <result property="dueDate" column="due_date"/>
        <result property="taskCondition" column="task_condition"/>
        <result property="memo" column="memo"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="TaskResultMap">
        SELECT * FROM task
    </select>

    <select id="findTaskById" resultMap="TaskResultMap">
        SELECT * FROM task WHERE id = #{id}
    </select>

    <insert id="insertTask" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task(title, start_date, due_date, task_condition, memo, status, created_at, updated_at)
        VALUES (#{title}, #{startDate}, #{dueDate}, #{taskCondition}, #{memo}, #{status}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateTaskStatus">
        UPDATE task
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="updateTask">
        UPDATE task
        SET title=#{task.title},
        start_date=#{task.startDate},
        due_date=#{task.dueDate},
        task_condition=#{task.taskCondition},
        memo=#{task.memo},
        status=#{task.status},
        updated_at=CURRENT_TIMESTAMP
        WHERE id=#{id}
    </update>

    <delete id="deleteTask">
        DELETE FROM task WHERE id=#{id}
    </delete>

</mapper>